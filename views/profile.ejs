<!DOCTYPE html>
<html lang="javascript">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>PostHub - Profile</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex flex-col">
  <header class="bg-zinc-800 p-4 shadow-md">
    <nav class="container mx-auto flex justify-between items-center">
      <a href="/" class="text-xl font-bold text-yellow-400">PostHub</a>
      <div>
        <a href="/logout" class="bg-red-500 hover:bg-red-600 px-4 py-2 rounded-md transition-colors">Logout</a>
      </div>
    </nav>
  </header>
  <main class="flex-grow container mx-auto p-6">
    <div class="mb-8">
      <h1 class="text-3xl font-semibold mb-2">Hello, <span class="text-yellow-400"><%= user.username %></span></h1>
      <p class="text-zinc-400">Welcome to your dashboard</p>
    </div>

    <div class="mb-8">
      <h2 class="text-2xl font-semibold mb-4">Create a New Post</h2>
      <form action="/post" method="post" class="space-y-4">
        <textarea
          class="w-full px-4 py-2 rounded-md bg-zinc-800 outline-none border-2 border-zinc-700 focus:border-yellow-400 transition-colors resize-none"
          name="content"
          id="content"
          placeholder="What's on your mind?"
          rows="4"
          required
        ></textarea>
        <input
          class="px-6 py-2 rounded-md bg-blue-500 text-white font-semibold cursor-pointer hover:bg-blue-600 transition-colors"
          type="submit"
          value="Create Post"
        />
      </form>
    </div>

    <div class="posts">
      <h2 class="text-2xl font-semibold mb-6 text-zinc-300">Your Posts</h2>
      <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
        <% user.posts.reverse().forEach(function(post) { %>
          <div class="post p-6 rounded-lg border-2 border-zinc-700 bg-zinc-800 hover:border-zinc-600 transition-colors">
            <h4 class="text-blue-400 font-semibold mb-3"><%= user.username %></h4>
            <p class="text-sm leading-relaxed mb-4"><%= post.content %></p>
            <div class="flex items-center justify-between mb-4">
              <small class="text-zinc-400"><%= post.likes.length %> likes</small>
            </div>
            <div class="flex gap-4">
              <a
                href="/like/<%= post._id %>"
                class="text-blue-400 hover:text-blue-300 transition-colors"
              >
                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
              </a>
              <a
                href="/edit/<%= post._id %>"
                class="text-yellow-400 hover:text-yellow-300 transition-colors"
              >
                Edit
              </a>
              <a
                href="/delete/<%= post._id %>"
                class="text-red-400 hover:text-red-300 transition-colors"
                onclick="return confirm('Are you sure you want to delete this post?')"
              >
                Delete
              </a>
            </div>
          </div>
        <% }) %>
      </div>
    </div>
  </main>
  <footer class="bg-zinc-800 p-4 text-center text-zinc-400 text-sm">
    &copy; 2024 PostHub. All rights reserved.
  </footer>
</body>
</html>
