<!DOCTYPE html>
<html lang="javascript">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>PostHub -  edit</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex flex-col">
  <header class="bg-zinc-800 p-4 shadow-md">
    <nav class="container mx-auto flex justify-between items-center">
      <a href="/" class="text-xl font-bold text-yellow-400">PostHub</a>
      <div class="flex gap-4">
        <a href="/profile" class="text-yellow-300 hover:text-yellow-500 px-3">Profile</a>
        <a href="/logout" class="bg-red-500 hover:bg-red-600 px-4 py-2 rounded-md transition-colors">Logout</a>
      </div>
    </nav>
  </header>
  <main class="flex-grow container mx-auto p-6 max-w-2xl">
    <div class="mb-8">
      <h1 class="text-3xl font-semibold mb-2">Edit Post</h1>
      <p class="text-zinc-400">Update your post content</p>
    </div>

    <form action="/update/<%= post._id %>" method="post" class="space-y-4">
      <textarea
        class="w-full px-4 py-2 rounded-md bg-zinc-800 outline-none border-2 border-zinc-700 focus:border-yellow-400 transition-colors resize-none"
        name="content"
        id="content"
        placeholder="Update your post content"
        rows="6"
        required
      ><%= post.content %></textarea>
      <div class="flex gap-4">
        <input
          class="px-6 py-2 rounded-md bg-blue-500 text-white font-semibold cursor-pointer hover:bg-blue-600 transition-colors"
          type="submit"
          value="Update Post"
        />
        <a
          href="/profile"
          class="px-6 py-2 rounded-md bg-zinc-600 text-white font-semibold cursor-pointer hover:bg-zinc-500 transition-colors"
        >
          Cancel
        </a>
      </div>
    </form>
  </main>
  <footer class="bg-zinc-800 p-4 text-center text-zinc-400 text-sm">
    &copy; 2024 PostHub. All rights reserved.
  </footer>
</body>
</html>
